name: money_transfer_app
description: Application mobile de transfert d'argent

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.4.0
  #cupertino_icons: ^1.0.3
  #flutter_svg: ^1.0.32
  #cupertino_icons_flutter: ^1.0.3
  # State Management
  get: ^4.6.5

  # Firebase
  firebase_core: ^2.15.0
  firebase_auth: ^4.7.0
  cloud_firestore: ^4.8.0

 # QR Code
  qr_flutter: ^4.1.0
  mobile_scanner: ^3.5.7

  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/